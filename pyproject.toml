[project]
name = "simplecadapi"
version = "2.1.1"
description = "A simplified OCP-native CAD modeling Python API"
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "倪旌哲", email = "nijingzhe@zju.edu.cn" }]
maintainers = [{ name = "倪旌哲", email = "nijingzhe@zju.edu.cn" }]
requires-python = ">=3.10,<3.14"
keywords = ["cad", "modeling", "3d", "design", "ocp", "opencascade"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "Intended Audience :: Manufacturing",
  "Topic :: Scientific/Engineering",
  "Topic :: Scientific/Engineering :: Visualization",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Operating System :: OS Independent",
]
dependencies = [
    "numpy>=1.21.0",
    "rich>=14.0.0",
    "cadquery-ocp>=7.9.3.1",
    "jsonschema>=4.25.1,<5.0.0",
    "rfc8785>=0.1.4,<0.2.0",
    "typing-extensions>=4.12,<5",
    "tomli>=2.0,<3; python_version < '3.11'",
    "py-slvs==1.0.6",
    "ocp-gordon>=0.2,<0.3",
    "ezdxf>=1.4.4",
    "pymupdf>=1.28.2",
]

[project.optional-dependencies]
dev = [
  "pytest>=7.0",
  "pytest-cov>=4.0",
  "mypy>=1.0",
  "black>=23.0",
  "isort>=5.0",
]
editor = []
inspect = [
    "matplotlib>=3.7.0",
]
gmsh = [
    "gmsh>=4.15,<5",
]
fem = [
    "gmsh>=4.15,<5",
    "ccx2paraview[VTK]>=3.2,<4",
]

[project.urls]
Homepage = "https://github.com/NiJingzhe/SimpleCADAPI"
Repository = "https://github.com/NiJingzhe/SimpleCADAPI"
Documentation = "https://github.com/NiJingzhe/SimpleCADAPI#readme"
Issues = "https://github.com/NiJingzhe/SimpleCADAPI/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
# Metadata-Version 2.5 is newer than what twine 6.x validates; keep the
# published metadata at 2.4 until the toolchain accepts 2.5.
core-metadata-version = "2.4"
# The Python package ships code only. Generated skill docs live in docs/skill
# (built by tools/auto_docs_gen.py + tools/skillbuild.py) and are distributed
# as the Agent Skill archive, not inside the wheel.
packages = ["src/simplecadapi"]
include = ["src/**/*"]

[tool.hatch.build.targets.sdist]
core-metadata-version = "2.4"
include = [
  "/README.md",
  "/LICENSE",
  "/pyproject.toml",
  "/MANIFEST.in",
  "/src",
  "/docs",
]
exclude = [
  "/.python-version",
  "/uv.lock",
  "/test",
  "/tests",
  "/skills",
  "/examples",
]

[dependency-groups]
dev = [
    "twine>=6.1.0",
    "pytest",
    "jupyterlab>=4.5.5",
    "ipykernel>=6.29.5",
    "matplotlib>=3.10.8",
    "numpy-stl>=3.2.0",
    "trimesh>=4.11.3",
    "datamodel-code-generator==0.32.0",
    "ty>=0.0.18",
]

[tool.ty.environment]
python-version = "3.10"

# FreeCAD runtime payloads are exec'd inside FreeCAD's embedded interpreter;
# GRAPH_NODES / App / Part etc. are runtime-injected globals by design.
[[tool.ty.overrides]]
include = ["src/simplecadapi/translator/freecad_translator/runtime/**/*.py"]

[tool.ty.overrides.rules]
unresolved-import = "ignore"
unresolved-reference = "ignore"

[tool.ty.rules]
# OCP/VTK are compiled C-extension bindings that ship no .pyi stubs, and
# example scripts import sibling modules — import resolution is
# environmental noise in this repo, not signal. Real import typos surface
# at runtime and in the test suite.
unresolved-import = "ignore"

[tool.ty.src]
include = ["src", "test", "tests", "examples", "tools", "viewer/server"]
exclude = [
    "**/__pycache__/**",
    ".venv/**",
    "build/**",
    "dist/**",
    "output/**",
]
[tool.pytest.ini_options]
testpaths = ["test", "tests"]

[project.scripts]
simplecad-cache = "simplecadapi.cache.cli:main"
simplecad-export = "simplecadapi.exporter.cli:main"
