# Session 审计报告：01a02469-2e59-7000-aa37-bb4361bd3080

**审计对象**：`~/.omp/agent/sessions/-Project-ocws-SimpleCADAPI/2026-08-21T13-01-08-057Z_01a02469-2e59-7000-aa37-bb4361bd3080.jsonl`
**任务**：依据参考图建模参数化弓形把手（沉头安装孔），后续追加导出、视觉复核、沉头螺栓安装性验证、Gmsh/CalculiX 有限元分析、连接处结构再设计。
**会话时长**：2026-08-21 13:01:08 → 17:23:35（4 小时 22 分，正常 dispose 退出，**任务未完成**——最后 40 分钟仍在修 QL 选边函数，L2078–L2087 之后无交付）。
**引用约定**：`L<N>` = JSONL 行号；时间为本地时间戳。

---

## 0. 会话总貌

| 指标 | 值 |
|---|---|
| 记录行 | 2093 行 / 11 MB |
| 用户发言 | 30 轮（其中 **21 轮为纠错/驳回**，见附录 B） |
| 助手消息 | 452 条；工具调用 786 次 |
| 工具分布 | read 373（含 **99 次 PNG 读图**、82 次 API 文档）、todo 104、edit 89、bash 88、eval 57、grep 47、write 29、glob 8、**ask 0** |
| 首个工具调用 → 首次宣告"已完成" | 13:03:03 → 13:13:52（10m49s，L9→L299） |
| 澄清提问（ask） | **0 次，全程** |
| 主模型推倒重建 | 3 次（v1 扫掠+圆盘座 → v2 重开目录重写 → v3 腰型座+QL），另有 ≥5 次局部拓扑推翻 |
| 编辑损坏文件事故 | 25 次（锚点漂移/残留重复块，附录 C） |
| Traceback/Segfault/SyntaxError | 26 次 |
| QL 首次被主动使用 | L2000（16:58），即开题后 **3 小时 55 分**，且发生在用户点名要求之后（L1977） |

**体积漂移轨迹**（同一把手的"最终实体"）：27785.5 → 24270.7 → 11289.3 → 9925.4 → 26509.5 → 10253.1 → 27489 mm³……设计在用户反馈驱动下反复震荡，无一次由力学计算主动触发。

---

## 1. 逐项问题取证

### P1 急于建模：不问设计意图与工况，造型凭经验

**证据链：**

- **20 秒定案**。首个工具调用 13:03:03（L9），建模简报 13:03:23（L29）即"已确定"：

  > "建模简报已确定：……由于图片没有尺寸，我会采用可替换的首版比例……外形会用圆截面弧形扫掠配圆角端座"（L29）

  参考图无尺寸、无工况说明（把手装在哪、受什么力、沉头朝哪面），skill 的 `requirement-refinement.md` 明确要求"image without stated dimensions is design intent, not a spec: establish scale … or **ask the one scaling question** when fit matters"。助手读了该文档（L9、L483 两次），**没有问那一个问题**，直接取经验值（孔距 140、弓高 26、沉头 2.4×孔径，L124）。全程 `ask` 调用为 0。
- **孔方向第一次错误**（凭经验把沉头做成 Z 轴、平板式安装）：v1 于 13:13:52 宣告完成并交付 STEP/FCStd（L299/L340），用户 13:22:19 一句话驳回："你的 孔方向就不对啊"（L417）。
- **孔方向第二次错误**：v2 重建时简报又写反（"沉头从正面 +Y 切入……把手厚度沿 Z"，L483/L610 文件头），用户 13:36:27 再次纠正："孔的轴线和 弓高方向是一样的"（L466）。
- **工况完全后置**。固定方式（沉头孔贴墙）、载荷（人拉力 +Y/−Z）直到 15:08:23 用户主动给出才进入模型（L1317）。此前 2 小时内造型决策（耳盘大小、耳厚、沉头深度）全部无受力依据；而工况一经给出，FEM 立刻暴露原结构 250N 下变形 2.32mm 不可接受（L1511 用户："你看这个型变量是不可接受的"）。
- **刚度修正 = 半径枚举**。位移超标后的响应是把 `ROD_RADIUS` 5.0→6.5（L1519）→8.5（L1612）连改两轮；全程 transcript 中**没有任何截面模量/弯矩/许用应力的估算记录**，纯试凑。250N→0.979mm、500N→0.643mm 的结果靠半径堆出来，耳座刚度、孔边应力、螺栓预紧等工况要素从未进入设计输入。

**根因归类**：模型能力（不主动澄清）+ skill 流程（refinement 规则存在但无强制 gate，"读了不等于问了"）。

---

### P2 "不读 API 文档"——实际是"读了但不按契约用"

公平修正：本 session 有 82 次 API 文档读取（`docs/api/*.md`），覆盖了所用 API。失败集中在**读文档只为找 API 名，不读签名/返回值/前置条件**：

| 事故 | 证据 | 文档本可避免？ |
|---|---|---|
| 调用不存在的 `Solid.is_closed()` | L614 首跑报错 | 是——`geometric-validation.md`（L18 已读）原文写着 "`Solid` has no public `is_valid()` method"，同页还在 L109 前再次读过 |
| 跑旧 example 脚本导出 STEP，而不是查 SDK 导出入口 | L1374 `uv run python examples/12_ap242_gmsh_volume_mesh/export_step.py`；用户 15:12:20 点破："ap242 是 老 example 你应该阅读 sdk 文档看 用什么导出"（L1388） | 是——`docs/api/export_product_package_to_step.md` 一直存在（L1397 才第一次读） |
| 旧包缺 `blobs` 字段导致导出失败 | L1386 | 部分——SDK 兼容性问题，但先重跑生成新包的判断正确 |
| gmsh `getNodes()` 返回三元组按二元解包 | L1457 | 是——外部 API，未查文档直接写 |
| `render_step_views_rpath` VTK 子进程段错误 | L358–359 | 否（环境/SDK 缺陷），但放弃后未再尝试替代官方路径，见 P3 |
| 点坐标重载 `tuple[float,...]` vs `Expr` 的类型边界反复 cast | L145、L801（"SDK 类型声明与其实际 ScalarLike 能力不一致"） | 否——SDK 文档/注解缺陷，助手只能在示例里 `cast` 绕过 |

**根因归类**：模型能力（浅读）+ SDK（类型注解与运行时行为不一致，L801 助手原话）。

---

### P3 视觉验证：相机靠猜、放大靠 zoom 枚举、高亮能力零使用

渲染共实际调用 47 次（`render_screenshot_rpath` 为主），读回 PNG 99 次。暴露的问题：

1. **相机/取景无框架能力，全靠手调**。
   - 首张官方截图相机几乎沿把手端部方向看，画面压成"竖向 C 形"不可用（L377 助手自认："我选的相机角度几乎沿着把手端部方向观察……不能用于和原图对照"）；默认视角列表还是靠读 SDK 源码 `render.py:48-65 DEFAULT_VIEWS` 才知道（L384–390）。
   - 47 次渲染中 39 次手填 `zoom` 数值，取值序列 3.0/3.2/3.4/3.5/4.0/4.2/4.2/5——纯枚举调参，没有按包围盒/感兴趣区域自动取景或裁剪。要做"根部放大图"时只能反复猜（L833–L912 一段连出 20+ 张 probe 图，最后被 `rm examples/out/arc_handle/probe_*.png` 清掉，L993）。
2. **关键对象高亮（`highlight_tags`/`tag_labels`/`show_callouts`）全程 0 次使用**。
   - `docs/api/render_screenshot_rpath.md` 第 6 行签名明确提供 `highlight_tags`、`tag_labels`；SDK 内部还有 `highlight_edge_width`、多色高亮组（`src/simplecadapi/inspect/brep/render.py:975-1044`）。全程没有任何一次调用提到 `highlight`（grep 证实 0 次）。
   - 直接后果：验证 fillet 时**无法把"我选中的边"画出来给自己看**——这正是 P4 中"选错 26 条边仍以为成功"长期不被发现的工具性原因。
3. **渲染证据链本身出过 bug**：FEM 云图 `vtkWarpVector.SetScaleFactor()` 放在 `Update()` 之后，导致"1x 实际比例图"和"20x 观察图"实际都是 20x 变形（L1582–L1588 定位、L1594 修复）。用户先发现"图中形变绝对不止这么点"（L1524），助手一度怀疑用户误读（L1526），自查后承认是自己的显示管线 bug（L1588）。
4. **多视角纪律时有时无**。L2030 用户："你看图了吗你的图根本无法体现 任何过度属性"——此前多轮"视觉复核"只给整机斜视图；L2032 才被迫定义"局部放大斜视 + 沿 Y 端面剖切 + 侧视"三图验收标准（用户逐图教育）。

**根因归类**：模型能力（不会用已有参数）+ SDK（渲染器无自动取景/ROI 裁剪/选中集高亮的易用入口，`zoom` 需手填数值）。

---

### P4 验证缺失与"验证定义错误"：操作成功 ≠ 效果符合预期

这是本次 session 最致命的一类问题，三个层次：

**(a) fillet 选错边照样成功，模型毫不怀疑。**

- L926 助手自认："前面的圆角失败是选了 26 条无关边"——即对 26 条非目标边做了圆角，内核返回成功实体，无任何告警；若非后续造型异常，此错误不会被发现。
- 修复方式是**暴力枚举**：`candidate_sets=[[0,1,2,3,4,6,8,10,11,12,13,14],…] × radius ∈ {0.5,0.8,1.0,1.2}`，且 `except Exception: pass` 静默吞错，eval 单元 30s 超时被 KeyboardInterrupt（L929–930）。
- 拿到"OK [0,1,…,14] 1.2 faces 25"后即写入正式模型（L939），并以"最终实体中有 8 个带 `face.arc_handle.root_blend` 标签的生成面"（L981）作为证据——**标签面数只证明圆角发生了，不证明圆角发生在正确的边上**。没有渲染高亮这 8 个面，也没有打印边的几何中心核对。
- 同类选边失效反复出现 6 次以上：L451（胶囊耳全边圆角后指纹不稳）、L1019（换切孔顺序后"旧选择器把沉头交线也当作根部边"）、L1628（杆半径改大后固定 1mm 阈值漏选→改 0.9×ROD_RADIUS）、L1747（鞍形套引入 20 条混合边，批量圆角全失败）、L1972（`get_edges()[0],[2]` 索引选择，1.0–3.5 半径全败）。

**(b) 验证定义错误：姿态无碰撞 ≠ 可安装。**

- 用户教了三步才到位：
  1. L1099 用户提出螺栓测试："在这个通孔里放一个略小一点的圆柱体做 螺栓测试，如果没有碰撞那就对了" → 助手实现 Ø5.3 圆柱**最终姿态**交集检查，通过（L1139）；
  2. L1154 用户："沉头孔安装的是沉头螺栓" → 升级为锥头包络（L1191 通过）；
  3. L1202 用户："要验证的其实是一个 沿着孔轴方向上的 包络没有碰撞。因为是要从上到下旋进去的" → 改为**轴向插入扫掠包络**，立刻失败：`ValueError: left countersunk-bolt insertion failed: swept-envelope overlap volume=163.845263265 mm^3`（L1214）。
- 即：此前两次"验证通过"的模型（L1139、L1191 交付）**实际都无法把螺栓装进去**。助手承认："之前的'最终安装姿态零碰撞'确实掩盖了这个问题"（L1216）。
- 反向错误也犯：L1954 发现把验证包络当切削体减掉导致两端大缺口（L1949 交付时未察觉，用户"continue"后才看出），于是**删掉切削**只留验证——L1957 用户随即点破原则："如果你的 包络只用来做碰撞，然后不做切割，试问你如何把一个沉头螺栓安装进去？"。验证几何与设计几何被割裂两次，两个方向都错。

**(c) 把用户反馈"再实现一遍"而不先验证理解。**

- L1009 用户："仔细看图，一部分 handler 遮住了孔"。助手将其解读为**设计意图**（"参考图中孔先形成于小安装台，手柄根部随后……会遮住孔口的一部分"，L1011），并真的实现了"杆根侵入孔口内侧扇区"，交付后两次被驳回（L1093"这个就是错的啊"、L1097"通孔怎么能挡住呢？"）。透视遮挡（视觉）与实体侵入（几何）未区分，直到用户给出可操作的判定实验（螺栓圆柱）才纠正。
- 该错误的几何版"验证"本来一眼可判：通孔里放圆柱测交集——但这要等用户提出。

**根因归类**：模型能力（无"操作效果回验"习惯）+ SDK（`fillet_rsolid` 无选中集回显/无逐边结果反馈；渲染无 highlight 联动，见 P3）。

---

### P5 可安装性与造型的关系：五轮试错全靠用户兜底

沉头孔安装性相关的时间线（全部为用户驱动的方向修正）：

| 时间 | 事件 | 驱动者 |
|---|---|---|
| 13:03–13:13 | v1：沉头沿 Z、双面、平板安装假设 | 经验（错） |
| 13:22 | "你的 孔方向就不对啊" | 用户 L417 |
| 13:36 | "孔的轴线和 弓高方向是一样的……注意 孔和 手柄的过渡方式" | 用户 L466 |
| 14:36–14:44 | "handler 遮住了孔"→ 耳厚→"扇区被吃掉不可接受"→ 螺栓测试 | 用户 L1009/L1021/L1093/L1099 |
| 14:49–14:58 | "如何定义沉头孔安装性的检查"→"沿孔轴包络、要旋进去" | 用户 L1154/L1202 |
| 14:58 | "你可以靠切来实现啊不一定要移动啊" | 用户 L1231 |
| 16:33–16:37 | 用户建议纯扫掠体端面打孔 → "沉头必须在圆弧面上，否则无法装入螺丝" | 用户 L1904/L1938 |
| 16:53 | "所有特征都是为了安装可行，受力可行和制造可行" | 用户 L1957 |

结构层面的连锁反应：杆根距孔中心仅 4.5mm < 螺栓头半径 5.4mm → 扫掠碰撞 163.8mm³（L1216）→ 移根（L1219）→ 用户建议改切削让位（L1231）→ 9925.4mm³ 让位通道版本（L1278）→ 纯扫掠版（L1943，端面沉头方向又错，L1938）→ 让位切除删掉又加回（L1954→L1957）→ 最终腰型座+包络切除（L2028+）。

**安装性从未作为造型约束进入设计**（沉头直径+杆径+最小壁厚 → 耳座最小尺寸 → 杆根偏置），而是作为事后验证不断打补丁。L1962 才第一次显式写出"杆根中心与孔中心的最小横向偏置 = 5.4 + 8.5 + 1.5 = 15.4mm"这类装配约束算式——且是在用户 L1957 立原则之后。

**根因归类**：模型能力（设计约束后置）为主。

---

### P6 QL：skill 明文要求，却 3 小时 55 分零使用

- **skill 规则明确**（`discipline/geometric-validation.md`，L18 即已读）：

  > "Select by tags, axes, normals, positions, or QL predicates — **not arbitrary topology indexes, which booleans and fillets renumber**."

  以及工作流第 5 步"Ground every step: print small QL-derived facts"。全程 452 条助手消息中，**第一次亲手写 QL 查询在 L2000（16:58）**，触发条件是用户 16:57:24 的点名："你不考虑是你的 选择做的不对吗，你应该用 ql 做选择"（L1977）。此前所有选边/选面：`get_edges()[i]` 索引（L1972）、手写中心阈值 `abs(x-lx)<12`（L641）、边长过滤（L926）、数量猜谜（expected 8 got 12，L2051）。
- **QL 上手后立刻暴露的能力断层**（用户原话"ql 能力不足"的具体化）：
  1. 首个谓词（`center.y>2.49 & length>40`）命中 3 条边，第三条是弧杆中部纵向接缝——**QL 给了结果但没有任何反馈说明"你选的可能是接缝"**，靠助手自己打印中心点排查（L2000–2003）；
  2. 收紧窗口选对 2 条边后 fillet 仍全败——根因是 0.5mm 环带容不下任何圆角（L2006），**"选对边"与"几何可行"是两件事**，QL 无法提示；
  3. 用户再提示"你可以在 ql 中表达相交的相交线"（L2008）→ 采用 `shared_boundary(..., to_kind="edge")` 后一次选中真实根边（L2016 验证"恰好一条"）——证明**"命名体 ∩ 命名体 的交线"正是正确抽象**，但由用户而不是文档/SDK 引导发现；
  4. 融合+圆角后**标签丢失**：`shared_boundary` 得到 0 面，被迫退回 `normal.y>0.9` 几何谓词（L2078–2081）；skill 文档写明 `apply_tag` 是 LOCAL 作用域"never propagates downward"，但跨布尔/圆角的存活语义实际可用性差；
  5. 基数假设连错两次：预期 8 实得 12（L2051）→ 改按 `geom.type=="CIRCLE"` 预期 2 实得 4（L2066）——**拓扑在操作后的真实形态缺少可预查的探针**，只能 `.exactly(N)` 硬猜后改数。
- **正面记录**：一旦用上，L2016/L2022 的 shared_boundary 选择稳定且 `exactly(1)` 语义明确；L2042 助手承认"QL 虽然选到了真实共享边，但'选对边'不等于'几何过渡正确'"——QL 解决选择，不解决设计。

**根因归类**：模型能力（规则在手不用）+ SDK（QL 无"选错反馈"：无命中预览渲染、无"这条边属于哪个面/由哪个操作产生"的溯源、`shared_boundary` 入口深、标签跨操作存活不稳）。

---

### P7 读图与完成度判断：四次"已完成"被用户用同一张参考图驳回

| 交付宣告 | 用户驳回（间隔） |
|---|---|
| L299 13:13 "已完成参数化弧形把手建模"（孔向错） | L417 13:22 "孔方向就不对啊"（+9min，含导出两步） |
| L1008 14:35 "已按反馈重做"（杆根吃掉孔口扇区） | L1009 14:36 "仔细看图，一部分 handler 遮住了孔"（**+27 秒**）；L1093/L1097 再驳 |
| L1833 16:2x "多截面鞍座已经把弧杆根部拉成连续的圆润过渡" | L1839 16:26 "你要不再重新看看图呢[222x207 小图] 这肯定不对啊"（鞍座方向建反+双圆片） |
| L1949 16:4x "单扫掠体构建通过"（读了渲染图仍宣称成功） | L1952 "continue" 后自己才看出两端被验证包络切出大缺口（L1954） |

- 13:17:44 用户第一次要求"请使用视觉反馈看结果是否正确和原始图是否一致"（L341）——**视觉对照是用户引入的验收方式**，此前 14 分钟的"验证"只有体积/面数/BRep。
- 读图错误模式：把投影遮挡当实体特征（L1011）、把结构方向建反（L1841 自认"右侧根部的过渡方向被我改反了……视觉上出现了两个堆叠的圆片"）、交付前读图但看不出明显缺口（L1949→L1954）。参考图始终只有一张 1568x1176，无尺寸、无多视角，但助手也未向用户索取正交视图或尺寸，只靠单图比例猜。

**根因归类**：模型能力（读图 + 完成度自评过度自信）。

---

### P8 FEM 执行质量（相对最好，但有方法论空洞）

**做对的**：
- 材料假设显式声明：3D 打印 PVC 取各向同性线弹性代表值 E=2500MPa，声明打印方向/填充率/层间强度影响（L1328）；
- 载荷分级：被用户质疑"人能产生 500N 拉力吗"（L1376）后给出 250/500/707N 三档并区分持续/峰值（L1380）；
- 网格敏感性：0.9mm vs 1.4mm 网格对比，峰值 597.2→479.7MPa（约 20%），如实标注"不能直接当作 PVC 允许应力"（L1495）；
- 边界/载荷面用 Gmsh 面质心按几何选出（y=−2.5 环形背面、弧杆中段带），不凭渲染猜（L1403）；
- 位移结论以 `.dat` 原始表核对，不受云图显示影响（L1529 起）。

**缺失的**：
- 597MPa 峰值 vs PVC 屈服（典型 45–55MPa）相差一个数量级，**报告从未回答"这零件到底行不行"**——没有许用应力/安全系数结论，热点只有位置没有判决；
- 结构迭代（半径、鞍座、腰型）与 FEM 脱节：L1833 计划"复跑 FEM 比较新旧"被视觉返工 block（L1841），此后至会话结束 FEM 再未运行；应力集中位置（左耳沉头让位区）没有反馈进任何一次再设计。

**根因归类**：模型能力（工程判决层缺失）。

---

### P9 过程性损耗：25 次把文件改坏（附带发现）

89 次 `edit` 中至少 25 次产生语法损坏/残留重复块（锚点漂移、旧函数尾残留、字面量 `CUT 358.=358` 留进源码、组合编辑被拒后部分应用，附录 C 全列表）。每次都靠"报错→重读→重写整块"恢复，平均损失 2–3 轮工具调用。这不是 CAD 问题，但显著压缩了有效迭代次数，也是"读图-改模-验证"循环变慢的放大器。

**根因归类**：harness/工具（edit 锚点机制）+ 模型（大块替换时未先读准边界）。

---

## 2. 归因矩阵

| # | 问题 | Skill 流程 | 模型能力 | SDK 设计 |
|---|---|:-:|:-:|:-:|
| P1 | 不澄清意图/工况，经验造型 | ●（refinement 无强制 gate，"应问不问"无拦截） | ●● | |
| P2 | API 文档浅读、契约性误用 | ○（API 页有，但无"签名陷阱"高亮） | ●● | ●（类型注解≠运行时） |
| P3 | 相机/放大/高亮不会用 | ○（工作流只写"Visual plausibility"一句） | ●● | ●●（无自动取景/ROI/高亮易用性） |
| P4 | fillet 选错边不疑、验证定义错 | ●（validation gate 只查体积/BRep，不查"选中集正确性"） | ●● | ●●（fillet 无选中回显；QL 无选错反馈） |
| P5 | 安装性不进设计约束 | ●（无"装配/安装可行性前置"条目） | ●● | ○ |
| P6 | QL 3h55m 零使用 | ●●（明文规则无 enforcement，示例太少） | ●● | ●●（shared_boundary 深藏、标签不跨操作存活、无命中预览） |
| P7 | 读图错误、完成度自评失真 | ○（无"交付前对照参考图清单"） | ●● | ○（无 A/B 叠图对比工具入口） |
| P8 | FEM 无判决结论 | ○（无 FEM 工作流页） | ● | |
| P9 | 25 次编辑损坏 | | ● | （harness） |

●● 主因 ● 次因 ○ 轻微

---

## 3. SDK 设计失败清单（本次实证）

1. **`fillet_rsolid` 静默成功于错误边集**：无 selected-edges 回显、无 before/after 面数对照报告、无"这些边相邻哪些面"的输出（L926 的 26 边事故靠造型异常才暴露）。
2. **渲染器验证工效学缺失**：`render_screenshot_rpath` 有 `highlight_tags`/`tag_labels`/`show_callouts` 却无人用（本次 0/47）；无按 bbox/ROI 自动取景，`zoom` 为裸数值；`render_step_views_rpath` 在本机 VTK 子进程段错误后无降级路径（L359）。
3. **QL 反馈回路缺失**：命中集只有对象无解释——没有"命中边的高亮渲染"快捷方式、没有边的来源面/来源操作溯源、没有基数不符时的最近候选拟合提示（expected 8 got 12 / expected 2 got 4 两次靠人肉改谓词，L2051/L2066）。
4. **拓扑选择稳定性**：`apply_tag` LOCAL 作用域导致跨 union/fillet 标签丢失（L2078 shared_boundary 得 0 面）；用户需要的"naming body ∩ naming body 交线"能力实际存在（`shared_boundary(to_kind="edge")`）但入口深、文档弱，靠用户口述才被启用（L2008）。
5. **持久化边界**：`Var/Expr` 进 primitive `geo` 元数据导致 `.scadpkg` 拒绝序列化（两次独立踩中：L185、L683；助手先后修了 `topology_snapshot.py` 与 `_operators_geometry.py`，并补了回归测试 `test/test_artifact_topology_snapshot.py`，L773/L795 `15 passed`）。旧包 manifest 缺 `blobs` 字段不兼容（L1386）。
6. **类型声明与运行时不一致**：点坐标注解 `tuple[float,...]` 不含 `Expr`、`apply_tag` 返回宽泛 `AnyShape`（L801），迫使用户代码满篇 `cast`。

---

## 4. Skill 流程改进建议（按证据直指）

1. **Refinement gate 硬化**：参考图无尺寸 + 涉及安装特征（沉头/螺栓/配合）时，把"载荷方向、安装面、装配方式、一个定尺寸"列为 brief 必填项；缺项必须 `ask`，一次问全。证据：本例 0 ask → 21 轮返工。
2. **选中集验证作为强制步骤**：任何 `fillet/chamfer` 前，打印 QL 选中集的基数+中心+相邻面标签，并用 `highlight_tags` 渲染一张确认图。证据：L926、L1019、L1747、L1972。
3. **验证定义三问写进 validation 章节**：验证的是姿态还是路径？验证体与设计体是否同一几何？验证失败时禁止"降级验证"只能改设计。证据：L1139→L1214（姿态通过/扫掠失败 163.8mm³）、L1954→L1957（删切削保验证）。
4. **安装性前置**：沉头螺栓章节给出包络公式（头径+杆径+行程→最小耳径/最小偏置），要求在造型前算出并写入 brief。证据：L1962 的 15.4mm 偏置算式直到第 4 小时才出现。
5. **视觉验收最小集**：整机三视图 + 问题区域局部放大（自动取景）+ 参考图并排对比 + 高亮选中集；交付前逐项打勾。证据：L1009（+27s 被驳）、L1839、L2030。
6. **FEM 判决模板**：峰值应力 vs 材料许用值的安全系数结论为必输出项；结构改动的 FEM 复跑与几何改动绑定。证据：L1495 有敏感性无判决；L1841 后 FEM 断线。
7. **QL onboarding**：workflow 中放一段"边选择的默认写法 = 语义面标签 + `shared_boundary`"的可复制范式，并声明索引选边为反模式（现在只在 discipline 页一句带过）。证据：L1977 之前零使用。

---

## 附录 A：三次重建与关键交付物时间线

```
13:03  开题；20s 出简报（0 澄清）
13:13  v1 完成 examples/arc_handle.py（孔轴向 Z，错）＋修 SDK Expr 序列化 bug
13:17  导出 STEP/FCStd；用户要求视觉对照
13:19  render_step_views 段错误 → 截图渲染；相机错（C 形）
13:22  用户：孔方向不对 → v1 废
13:36  用户给坐标约定；v2 重开 examples/arc_handle/main.py
13:41  v2 完成（大耳盘+球过渡）＋再修 _operators_geometry.py 元数据求值
14:01  用户：过渡不平滑、耳盘太大 → v2'
14:36  小耳版交付 → 27s 后被驳（遮孔）
14:44  用户教螺栓测试；14:49-14:58 教沉头包络与旋入扫掠
14:57  扫掠验证失败 163.8mm³ → 移根/让位切削
15:08  FEM 任务；15:12 用户纠正导出入口
15:23  变形超标 → 半径 5→6.5→8.5
16:09  连接处再设计：圆锥套→球→5 截面鞍座（全部视觉否决）
16:26  用户：重新看图（鞍座错）
16:31  用户：腰型；16:34 用户：纯扫掠；16:37 用户：沉头必须在弧面
16:53  用户：包络必须参与切割（安装/受力/制造可行）
16:57  用户：用 QL；16:59 用户：shared_boundary
17:05  用户：图无法体现过渡属性
17:06  腰型座+QL 版 main.py；17:19 仍在修 QL 选边函数（标签丢失→法向谓词）
17:23  session dispose（未完成交付）
```

## 附录 B：用户 30 轮发言分类

- 纠错/驳回类 21 轮：L300(导出指令)/L341(引入视觉验收)/L417/L466(孔向)/L819(耳盘)/L1009(遮孔)/L1021(耳厚)/L1093/L1097/L1099(通孔不可挡+测试方法)/L1154/L1202(安装性定义)/L1231(用切削)/L1352/L1388(工具与文档)/L1511(变形)/L1524(位移显示)/L1664(连接处)/L1839(看图)/L1890(腰型)/L1904(纯扫掠)/L1938(沉头在弧面)/L1957(特征三可行性)/L1977(用QL)/L2008(QL交线)/L2030(图无过渡属性)
- 纯指令 4 轮（导出/FEM/use uv run/continue）；追问 1 轮（500N 可信度）；方法授予 5 轮（视觉反馈、python kernel 探索、螺栓测试、腰型/扫掠建议、QL/shared_boundary）。
- **结构性事实：用户在本 session 中承担了设计评审、安装性定义、结构选型（腰型/扫掠）、工具选型（QL/交线选择）四重角色。**

## 附录 C：25 次编辑损坏事故（行号）

L208, L442, L625, L647, L1040, L1118(组合编辑被拒), L1130, L1176, L1248, L1281(拒绝), L1418, L1442(字面量 CUT 残留), L1463, L1591(拒绝), L1615(拒绝), L1729, L1738, L1759, L1865, L1936；另 L185/L683 为两次独立 SDK 持久化 bug 修复（非事故）。

## 附录 D：方法说明

- 解析 JSONL 全量 2093 行，抽取 assistant text/toolCall、user、toolResult 与 custom 事件共 1055 个时序事件；统计与引文均来自原文字段，未做语义改写。
- "首次 QL 使用 = L2000"由全量 arguments 正则匹配 `from simplecadapi import ql|ql\.(edges|faces|prop|and_|or_|tag|shared)` 判定；此前命中均为 toolResult 中他人代码/文档文本（L26/L543/L1888 已逐条排除）。
- 渲染参数统计基于 bash/eval 体内 `render_*` 调用的字面参数；`highlight` 0 次由全部 786 次工具调用参数 grep 证实。
- 两次 compaction（L1153、L1780）摘要已读取，与正文事件无冲突。
